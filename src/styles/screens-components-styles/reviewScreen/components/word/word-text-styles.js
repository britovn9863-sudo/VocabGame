import { GLOBAL_COLORS } from "../../../../colors/global-colors.js"
import { DEFAULT_FONT_FAMILY } from "../../../../fonts/default-font-family.js"

export const wordTextStyles = {
  desktop: {
    fontFamily: DEFAULT_FONT_FAMILY,
    fontSize: 40,
    fill: GLOBAL_COLORS.WHITE
  },
  mobile: {
    fontFamily: DEFAULT_FONT_FAMILY,
    fontSize: 40,
    fill: GLOBAL_COLORS.WHITE
  }
}
